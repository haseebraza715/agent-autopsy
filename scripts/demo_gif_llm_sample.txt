Summary
Retry loop on web_search — Events 3–7 repeat the same failing calls.

Root cause
No retry cap; empty tool responses did not stop the graph.

Fixes
• Cap retries + exponential backoff on web_search
• Surface “empty output” as an error before the next LLM turn

Confidence
0.82 (matches repeated tool_call + error rows in the trace)

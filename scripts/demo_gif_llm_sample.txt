## Summary
Run `run_loop_001` failed after a tight retry loop: `web_search` returned empty or errored and the agent kept issuing the same class of tool calls without backoff (see Events 3–7).

## Root cause
No circuit breaker or retry cap on `web_search`; identical queries were retried without validating tool output before the next LLM step, so the graph never exited the failure path.

## Fix recommendations
- A) Code: cap retries per tool name and add exponential backoff.
- B) Ops: surface tool latency/empty payloads to the model as explicit errors.
- C) Prompt: require a plan change after two identical failed tool calls.

## Confidence
0.82 — pattern matches repeated tool_call rows with escalating errors in the trace.

# Streamlit Cloud installs dependencies from this file by default.
# Install the local package with the extras needed by the web app:
# - base package deps (pydantic, typer, rich, httpx, watchdog, etc.)
# - Streamlit UI
# - optional LLM providers used by the app when configured
.[gui,llm]
